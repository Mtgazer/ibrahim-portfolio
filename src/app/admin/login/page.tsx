/*
  /admin/login — admin sign-in page.

  Email + password authentication via Supabase Auth will be
  implemented in Phase 4. Session management and redirect
  logic will also be added at that time.
*/
export default function AdminLoginPage() {
  return (
    <main>
      {/* Login form — Phase 4 */}
    </main>
  );
}
