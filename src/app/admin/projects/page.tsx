/*
  /admin/projects — project management list.

  Displays all projects with publish/feature toggles and delete controls.
  Full implementation in Phase 5.
  Requires: Supabase (Phase 3), authentication (Phase 4).
*/
export default function AdminProjectsPage() {
  return (
    <main>
      {/* Project list — Phase 5 */}
    </main>
  );
}
